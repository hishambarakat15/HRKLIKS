import { ScrollToSectionDirective } from './scroll-to-section.directive';

describe('ScrollToSectionDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollToSectionDirective();
    expect(directive).toBeTruthy();
  });
});
